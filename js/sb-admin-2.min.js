/*!
 * Start Bootstrap - SB Admin 2 v4.1.3 (https://startbootstrap.com/theme/sb-admin-2)
 * Copyright 2013-2021 Start Bootstrap
 * Licensed under MIT (https://github.com/StartBootstrap/startbootstrap-sb-admin-2/blob/master/LICENSE)
 */

(function($) {
    "use strict";

    // Toggle the sidebar on button click
    // $("#sidebarToggle, #sidebarToggleTop").on("click", function(e) {
    //     $("body").toggleClass("sidebar-toggled");
    //     $(".sidebar").toggleClass("toggled");

    //     // Collapse sidebar if it's toggled
    //     if ($(".sidebar").hasClass("toggled")) {
    //         $(".sidebar .collapse").collapse("hide");
    //     }
    // });

    // Collapse the sidebar on window resize
    $(window).resize(function() {
        if ($(window).width() < 768) {
            $(".sidebar .collapse").collapse("hide");
        }

        // Toggle sidebar if window width is less than 480
        if ($(window).width() < 480 && !$(".sidebar").hasClass("toggled")) {
            $("body").addClass("sidebar-toggled");
            $(".sidebar").addClass("toggled");
            $(".sidebar .collapse").collapse("hide");
        }
    });

    // Handle scrolling when the sidebar is fixed
    $("body.fixed-nav .sidebar").on("mousewheel DOMMouseScroll wheel", function(e) {
        var o;
        if (768 < $(window).width()) {
            // Normalize wheel delta
            o = (o = e.originalEvent).wheelDelta || -o.detail;
            this.scrollTop += 30 * (o < 0 ? 1 : -1);
            e.preventDefault();
        }
    });

    // Show/hide the scroll-to-top button based on scroll position
    $(document).on("scroll", function() {
        if ($(this).scrollTop() > 100) {
            $(".scroll-to-top").fadeIn();
        } else {
            $(".scroll-to-top").fadeOut();
        }
    });

    // Scroll to top when the scroll-to-top button is clicked
    $(document).on
